# Default repo - useful for R CLI
local({
  r <- "https://cloud.r-project.org"
  options(repos = r)
})
# Suppress warnings when !expr values are in R Markdown yaml header
options(yaml.eval.expr = TRUE)
